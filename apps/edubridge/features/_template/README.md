# Feature module template

Copy this folder to `features/<module>/` when starting a new module. See `docs/guides/feature-folder-structure.md`.

```
features/<module>/
├── components/
├── hooks/
├── queries/
├── actions/
├── lib/
├── types.ts
├── index.ts
└── README.md
```
