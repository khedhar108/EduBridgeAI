# Taste
- When onboarding to a codebase or starting exploration, reads `.cursor/rules` and project documentation first, before examining source code. Confidence: 0.8
- Prefers brief, concise overviews of a codebase rather than exhaustive detail dumps. Confidence: 0.7
- Likes decorative hairline-grid background overlays (radial-masked grid pattern) across the entire marketing homepage — not just the hero — layered behind content via z-index; this is an intentional design choice, not an accident. Confidence: 0.9
- Wants explicit user/in-repo design choices to win over generic taste-skill bans — pushed back when a skill default (no decorative grid) removed a feature they liked, and expects it restored. Confidence: 0.8
- Requires explicit user permission before running build commands (they are slow); lint and type-check may run freely without asking. Wants this encoded as a rule, not just an ad-hoc habit. Confidence: 0.95
- Uses both Cursor and Command Code in the same repo and wants behavioral rules mirrored in both systems (e.g., AGENTS.md and `.cursor/rules` for Cursor, `.commandcode/settings.json` for Command Code). Confidence: 0.9
- Prefers reverting to the default/system font over keeping a Google-hosted web font that fails to download — wants no network dependency for typography; a font that doesn't reliably load should be dropped rather than worked around. Confidence: 0.7
- Wants to understand the root cause of warnings/errors ("tell me why"), not just have them fixed silently. Confidence: 0.75
- Wants repo knowledge (e.g., Node/runtime version behavior on deployment platforms) captured as written documentation, and wants the document's placement confirmed with them before writing it. Confidence: 0.8
- Cares that environment/runtime fixes hold on deployment platforms (Vercel, Netlify, CI), not just locally — asks how a change will behave in deployment before applying it. Confidence: 0.6
- Before entering plan mode, surface any relevant skills in the project (`.agents/skills/`) and ask the user to opt in rather than auto-selecting them. Confidence: 0.85
- When asked to enumerate things (e.g., available skills), prefers a short, scannable list over verbose detail. Confidence: 0.6
- Prefers a lean, minimal taste/rules set — pushes back against creating many rules and only wants genuinely necessary entries rather than exhaustive coverage. Confidence: 0.7
- Wants taste files to avoid duplicating conventions already defined in AGENTS.md and `.cursor/rules`; if a rule/convention already lives there, don't re-record it in taste. Confidence: 0.75
- Prefers a clean, non-nested taste directory (rejected the awkward `taste/taste/taste.md` nesting) organized under a project-named category folder (e.g., `edubridge/taste.md`). Confidence: 0.7
- Prefers the layered documentation reading order (root README → docs/README.md → docs/roadmap/README.md → active phase file → one task-specific doc), stopping once enough context is gathered and never bulk-reading. Confidence: 0.7
- Values architecture and documentation decisions being validated against industry standards and best practices, and treats agent-friendliness and scalability as explicit design criteria when evaluating such plans. Confidence: 0.6
- Cares about documentation staying consistent with code — when an app/path is renamed (e.g., `apps/web` → `apps/edubridge`), wants stale doc references swept and corrected rather than left to drift, to preserve docs as a single source of truth for agents. Confidence: 0.7
- When sweeping a rename, prefers semantically-aware replacement over blanket find-and-replace — keeps historical records (ADRs, build logs) and references that genuinely mean the old entity (e.g., the demo app) unchanged. Confidence: 0.6
- When a task involves an unfamiliar technical term or acronym, wants a brief definition/explanation up front (e.g., "what does ADR mean?") before the related change proceeds, rather than having the assistant act without clarifying. Confidence: 0.55
- Prefers building new visual components on a standalone preview route first (verify in isolation), then integrating into the main page — not building directly in the target surface. Confidence: 0.85
- Wants rich, voice-driven marketing copy on home and marketing surfaces — explicitly stated the home page text "must not just simple"; expects hook/bridge/CTA-style copy tied to the design concept, not plain functional text. Confidence: 0.8
- Wants a fresh, distinctive visual look on marketing surfaces while still respecting the established design system — not a copy of the current design, but a new interpretation within its constraints. Confidence: 0.75
- Prefers self-contained, encapsulated components that own their internal implementation (fallbacks, reduced-motion, lazy loading, static states) so integration into a parent page is a trivial one-tag swap rather than scattered inline wiring. Confidence: 0.75
- Wants folder/file-tree structure diagrams included when planning new component creation — explicitly asked to "show me the folder structure" during plan review. Confidence: 0.7
- When e2e/manual testing is needed for flows that touch live external auth providers (e.g. Supabase Auth), wants the assistant to autonomously handle all non-browser preparatory work (creating test users, seeding data, running DB scripts, verifying state) so the user only does the browser-click-through part. Initially accepted a runbook-only approach but then explicitly asked the assistant to perform the setup steps itself ("you should perform steps one, two, three so I can test manually"). Still does not want agent-automated browser testing that creates side effects on shared dev infra. Confidence: 0.8
- Prefers status reports and next-steps guidance presented as structured markdown tables — explicitly asked "Create a table and tell me the next step what I should follow." Table columns like step/phase/owner/state work well. Confidence: 0.8
- Wants a clear split between "what we have achieved" and "what's next" when asking for project status, with emphasis on actionable next steps rather than exhaustive retrospective detail. Confidence: 0.7
- Values finishing incomplete foundational work (e.g. partial auth flows) before starting new feature phases — explicitly asked whether partial work should have been completed first, and accepted the recommendation to not skip ahead to later-phase features. Confidence: 0.6
- Wants an upfront assessment of whether the agent can handle a task fully autonomously or if manual human steps (e.g. running migrations, configuring auth provider settings) are required first — asks "can you create all this or I have to perform some migrations command manually first." Prefers this boundary stated before work begins, not discovered mid-task. Confidence: 0.75
- Wants seed/dev data properly distributed across all relevant tenants (e.g. every school should have its own set of seeded users/members), not just one tenant — "you must feed data properly in relevant schools." Expects multi-tenant completeness in seed scripts. Confidence: 0.65
- Likes varied, multi-hue accent colors across UI elements (e.g., orbiting feature cards) over strict monochrome teal — willing to deviate from a strictly-teal design plan for more color variety, but always keeps the light theme. Confidence: 0.85
- Demo/dev-only UI affordances (like a demo-accounts modal) must be gated by `process.env.NODE_ENV !== "production"` so they never ship to production — treats the dev/prod boundary as a hard requirement for test tooling. Confidence: 0.7
- Wants proper entrance animations on bento/module-card grids on marketing pages (e.g., staggered reveal), not just hover states. Confidence: 0.65
- Prefers 3D/WebGL treatment for the brand mark (the drop) over flat SVG — wants it to read as a real 3D object rather than a static shape. Confidence: 0.6
- Wants the brand mark (drop) centered inside the black hole/orbit well as the focal point, rather than placed below or off to the side of the orbital animation. Confidence: 0.7
- Wants a tagged git checkpoint committed before major code changes so the work can be reverted if the new version has issues — explicitly asked to "make sure we have a proper checkpoint so we can revert back." Expects the revert command to be stated after the commit. Confidence: 0.9
- During iterative rework phases, prefers old/replaced code to be commented out rather than deleted — wants to verify the new version works before removing the old code. Cleanup of commented-out blocks happens only after explicit confirmation. Confidence: 0.85
- Prefers code changes to be scoped to only the relevant files during iterative rework — explicitly asked to "only modify the relevant files of code" rather than touching unrelated areas. Confidence: 0.7
- When the user explicitly names a skill in a request (e.g., `/ui-ux pro max`, `/frontend-design`), applies that skill directly rather than re-asking for opt-in; opt-in prompting applies only to auto-discovered skills the user hasn't named. Confidence: 0.7
- Uses `motion` (Motion/react) for entrance/reveal animations and respects `prefers-reduced-motion` for decorative motion — mesh blobs and gradient bars are CSS keyframes gated behind the media query, and existing motion components use `useReducedMotion`. Confidence: 0.6
- Wants the brand drop mark to faithfully match the brand thumbnail — the thumbnail is the purple/multicolor drop (purple outer bands, teal/mint inner bands, amber pulse), and the hero should use that exact version. Corrected an earlier teal-only request: the brand mark itself is multicolor, not teal-only; multi-hue accents elsewhere (orbit/bento cards) are fine. Confidence: 0.85
- Before creating a new asset or component variant, wants existing variants checked/enumerated first (e.g., "can you check we have multiple SVG files for it?") rather than generating a duplicate without looking. Confidence: 0.6
- Wants the brand asset set to remain clean — no dead/unused brand files kept around. Confidence: 0.7
- Wants cards on the home page and module surfaces to be eye-catching with varied light/pastel background colors, not uniform same-colored flat cards — explicitly asked to move away from cards that are "only with light and same colors" toward beautiful, distinct light tints (a curated multi-hue pastel set) while staying light-theme. Confidence: 0.85
- Prefers using proper shadcn registry components for UI primitives when explicitly requested (e.g., invoked /shadcn and asked to "use shad CN properly" for BentoGrid). However, do NOT proactively add new shared components (BentoGrid), tint tokens, or new dependencies to the @repo/ui package just for a color change — the user learned from the BentoGrid incident that shared infrastructure should not be added unless explicitly asked. Confidence: 0.8
- When a design direction changes, expects the design plan and the taste files to be updated together — asked to "update the design plan regarding this and taste as well." Confidence: 0.65
- When restyling shadcn registry components to match the project design system, removes unused dependencies the CLI auto-adds (e.g., @radix-ui/react-icons when the project is lucide-only) and replaces raw utility colors with semantic tokens — expects added components to be fully token-compliant, not left with defaults. Confidence: 0.7
- When the user asks to make cards/menus more colorful, keep the change local to the feature files — use arbitrary inline oklch pastels directly in the marketing/feature code (matching the hero precedent), do NOT add shared tint tokens, new components, or new deps to the @repo/ui package unless explicitly asked. Keep the existing hand-rolled grid rather than replacing it. Confidence: 0.85
- For SSR/hydration mismatches caused by non-deterministic rendering inside third-party packages (e.g., `Math.random()` arm poses in `feral-blob`'s JellyBlobMascot), never patch `node_modules` — fix in our own feature code by gating the component behind a `mounted` state flag so it renders client-only after hydration. Client-only rendering is acceptable for purely decorative `aria-hidden` components since there's no SEO/SSR cost. Confidence: 0.75
- For genuine third-party library bugs that must be fixed upstream (e.g., feral-blob's `value-not-animatable` opacity warnings from variant-animated elements missing `initial: false`), prefers durable `pnpm patch` (`.pnpm_patches` dir + `patchedDependencies`) over ad-hoc `node_modules` edits, so the fix survives reinstalls. With pnpm ≥9 the `patchedDependencies` entry must live in `pnpm-workspace.yaml`, NOT `package.json` (pnpm ignores it there). Confidence: 0.7
- After applying a bug fix, verify the change compiles using the app-scoped typecheck script (e.g., `pnpm --filter edubridge check-types`) rather than assuming the edit is correct. Typechecks/lints may run freely without asking permission. Confidence: 0.6
- Never leave dependency versions unpinned (`"latest"`, `"*"`) in workspace package.json files — unpinned Mastra ranges caused lockfile drift, dangling node_modules symlinks, and broken `pnpm run dev` after a filtered install; user explicitly said "In future, I don't want any version issues throughout application." Pin to exact versions (or move to the pnpm catalog) and run a full-root `pnpm install` after anything touches the lockfile. Confidence: 0.85
- When a runtime/dev-server problem is reported, run the command itself (e.g., `pnpm run dev`), read the actual output/logs, and inspect ports/processes to find the real cause — user asked for exactly this twice ("run pnpm run dev in the root and check is there any issue", "can you run pnpm run dev and read the output and solve the issue"). Prefers targeted root-cause diagnosis (stale port-holding processes, dangling symlinks) over blind `node_modules` wipes and full reinstalls. Confidence: 0.75
- Wants gradient visuals on the auth surfaces and keeps escalating the gradient ambition — said "i would like to implement gradient", then asked to convert the signup page into a card with two parts featuring animated gradient bars, then asked to add a mesh gradient. Accepted style: layered, token-derived washes (`var(--accent)` canvas + `color-mix(in oklch, …)` glows) that stay within the light-only, single-accent design system, with gradients confined to auth/marketing surfaces rather than the tenant workspace. Expect rapid design-direction changes on auth pages (split-screen → centered card) without ceremony. Confidence: 0.8
- Wants a button on the signup/auth page that opens a modal listing demo login credentials (emails + passwords) for the different roles, so reviewers can log in as any role directly from the UI instead of digging through seed scripts or docs. Clicking a role should auto-fill the sign-in form fields in place ("fill in the box for signin smoothly"), not just display the credentials. Confidence: 0.8
- When giving a loosely-specified task ("read doc you get the idea"), expects the assistant to derive the missing details from the project's own docs (seed files, READMEs, design docs) rather than asking the user to spell them out. Confidence: 0.65
